{
  "name": "lexstudent",
  "private": true,
  "version": "1.0.0",
  "description": "LexStudent monorepo — web app and desktop (Tauri) app",
  "scripts": {
    "web:install": "cd web && npm install && cd client && npm install && cd ../server && npm install",
    "web:dev": "cd web && npm run dev",
    "web:client": "cd web/client && npm run dev",
    "web:server": "cd web/server && npm start",
    "desktop:install": "cd desktop && npm install",
    "desktop:dev": "cd desktop && npm run tauri dev",
    "desktop:build": "cd desktop && npm run tauri build",
    "install:all": "npm run web:install && npm run desktop:install"
  }
}
